import {StyleSheet} from 'react-native';
import UI from '../../../ui';

export const styles = StyleSheet.create({
  container: {
    padding: 10,
    borderRadius: 10,
    borderWidth: 1,
    borderColor: UI.COLORS.BLACK,
  },
});
