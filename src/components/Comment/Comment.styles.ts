import {StyleSheet} from 'react-native';
import UI from '../../ui';

export const styles = StyleSheet.create({
  container: {
    paddingVertical: 16,
    borderBottomColor: UI.COLORS.BLACK,
    borderBottomWidth: 1,
  },
  row: {
    flexDirection: 'row',
  },
});
